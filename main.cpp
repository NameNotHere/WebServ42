#include "ConfigParser/Configuration.hpp"
#include <iostream>

int main()
{
	ServerConfig con;

	/*while (true)
	{
	    poll(allSockets);

	    for (each socket)
	    {
	        if (socket == server_fd)
	        {
	            // New client wants to connect
	            accept();
	        }
	        else if (socket has data)
	        {
	            // Existing client sent HTTP data
	            recv();
	        }
	    }
	}
	*/
}

